/**
 * @param {string} s
 * @return {string}
 */
 var longestPalindrome = function(s) {
    // 方法一： 动态规划
    // 先构造
    let len = s.length;
    let dp = new Array(len, false);
    for(let i=0;i<len;i++) {
        dp
    }

};